import SingleContainer from "../containers/SingleContainer";

const SearchDetailsScreen = ({navigation, route}) =>  <SingleContainer navigation={navigation} route={route} />

export default SearchDetailsScreen