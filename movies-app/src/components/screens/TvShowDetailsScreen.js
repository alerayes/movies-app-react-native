import SingleContainer from "../containers/SingleContainer";

const TvShowDetailsScreen = ({navigation, route}) =>  <SingleContainer navigation={navigation} route={route} />

export default TvShowDetailsScreen